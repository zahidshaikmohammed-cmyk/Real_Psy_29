services:
  - type: web
    name: psy29-live-data-hardening
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: python runner_live.py
    autoDeploy: true
    healthCheckPath: /health
